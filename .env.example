# Application
APP_ENV=local
APP_HOST=127.0.0.1

# Listen port (X_LISTEN or PORT)
X_LISTEN=8007
# PORT=8007

# Database (SQLite)
DB_DATABASE=database/database.sqlite

# JWT for API auth
JWT_SECRET=change-me-in-production
